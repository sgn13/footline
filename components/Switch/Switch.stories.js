import React from 'react';

import Switch from '.';

export default {
  title: 'Switch',
  component: Switch
};

const Template = args => <Switch {...args} />;

export const Default = Template.bind({});
