import React from 'react';

import Search from '.';

export default {
  title: 'Search',
  component: Search,
};

const Template = (args) => <Search {...args} />;

export const Default = Template.bind({});
